export type ConnectedFriend = {
  username: string;
  imageUrl: string;
  userId: string;
};

export type LoginData = {
  accessToken: string;
  id: string;
  username: string;
  initialized: boolean;
  imageUrl: string;
};
